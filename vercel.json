{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd main && npm install && npm run build",
  "outputDirectory": "main/dist",
  "framework": "vite"
} 